name: Tests

on: push

jobs:
  tests:
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@v2
      - uses: docker://bknguyen/mathsnuggets:latest
        with:
          args: "lint tests"
